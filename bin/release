#!/usr/bin/env bash
# bin/release <build-dir>

cat <<EOF
---
default_process_types:
  web: ">&2 echo 'Please use a Procfile to specify a default start command for this buildpack' && exit 1"
EOF

exit 0
